.headline{
    position: absolute;
    margin-top: 35%;
    z-index: -1;
    display: flex;
}
.headline__right{
    line-height: 1em;
    font-size: 64px;
}
.headline__left{
    font-size: 24px;
    color: $text-color;
    text-align: right;
}