.comments__slider{
    margin: 0 auto;
    margin-top: 110px;
    @include phones{
        margin-top: 35px;
    }
}
.slider{
    position: relative;
    height: 500px;
    overflow: hidden;
}
.slider__list{
    position: absolute;
    min-width: 100%;
    overflow: hidden;
    display: flex;
    white-space: norwrap;
    @include tablets{
        width: 100%;
    }
}